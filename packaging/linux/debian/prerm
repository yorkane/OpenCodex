#!/bin/sh
# 移除前先停服务，避免 dpkg 删掉正在运行的文件。
set -e

case "$1" in
  remove|deconfigure)
    systemctl stop opencodex-gateway.service >/dev/null 2>&1 || true
    systemctl stop opencodex-xvfb.service >/dev/null 2>&1 || true
    ;;
esac

exit 0
